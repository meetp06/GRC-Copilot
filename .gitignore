.env
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
*.log
runs/
.aws-sam/

# derived: rebuildable with `python -m src.rag.index build`
data/index/

# checkpoint database: run state, rebuildable, may contain customer text
data/runs.sqlite
data/*.sqlite

# parsed output and source PDFs: derived / third-party, rebuildable
data/policies_real/
pdf_data/

# dagster local run storage
.dagster_home/
tmp*/
history/
logs/

# ontology: 10MB NIST catalog (re-downloadable) and derived db
data/ontology/*.json
data/ontology/*.sqlite

data/jobs.sqlite
data/telemetry.sqlite
