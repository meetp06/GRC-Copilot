[pytest]
# Without pythonpath, `pytest` fails to import `src` while `python -m pytest`
# succeeds -- the module form puts the working directory on sys.path and the
# console script does not. Pinning it here makes both invocations behave.
pythonpath = .
testpaths = tests
